@model Playlister.ViewModels.HomeViewModel

@{
    ViewData["Title"] = "Home";
}

<!DOCTYPE html>
<head>
    <title>Home</title>
</head>
<div>
    @foreach ((string displayText, string action, string controller) in Model.Links)
    {
        <div>@Html.ActionLink(displayText, action, controller)</div>
    }
</div>
